extends Control

# The receiver of the one connection signal_container.tscn stores. A method the
# scene names has to exist, or the engine drops the connection on load and the
# fixture would prove nothing.
func _on_play_pressed() -> void:
	pass
